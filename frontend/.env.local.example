# URL du backend (laisser vide en dev local avec Next.js proxy, ou mettre l'URL Render en prod)
NEXT_PUBLIC_API_URL=https://mykasa-backend.onrender.com

# Utilisé uniquement côté serveur Next.js (SSR) — pas exposé au client
BACKEND_URL=http://127.0.0.1:8002
